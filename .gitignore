node_modules/
data/
dist/
web/dist/
*.log
.DS_Store

# .NET build output for the pp helper
tools/**/bin/
tools/**/obj/
tools/pp/
# The helper being built, and the one it replaces, while build:pp:local swaps them.
tools/pp.building/
tools/pp.old/

# Read-only reference checkout of ppy/osu-web (AGPL). Never committed: what the app uses from
# it arrives through scripts/build-osu-web-art.mjs, unedited and credited, not as a copy of
# their tree. Recreate it with the clone command in docs/osu-web-fidelity.md.
reference/

# go-winres output, made and removed by scripts/build-launcher.mjs
tools/launcher/*.syso
